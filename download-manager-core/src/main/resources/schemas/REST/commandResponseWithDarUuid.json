{
	"title": "REST Interface Command Response with Data Access Request UUID",
	"type":"object",
	"javaType": "int_.esa.eo.ngeo.downloadmanager.rest.CommandResponseWithDarUuid",
	"description":"The response provided as a result of an 'add dar' command sent to the Download Manager, including the DAR UUID.",
	"required":true,
	"extends" : {
		"$ref" : "commandResponse.json"
	},
	"properties": {
		"darUuid": {
			"type":"string",
			"description":"The UUID of the added DAR if the add command was successful.",
			"required":false
		}
	},
	"additionalProperties": false
}
